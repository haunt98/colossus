name: Docker

on:
  push:
    branches: [master]

jobs:
  storage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker build and push
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: cyndie7216/storage
          tags: latest
          dockerfile: docker/storage/Dockerfile
          cache_froms: cyndie7216/storage:latest

  gatewayserver:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker build and push
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: cyndie7216/gatewayserver
          tags: latest
          dockerfile: docker/gatewayserver/Dockerfile
          cache_froms: cyndie7216/gatewayserver:latest

  aiserver:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker build and push
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: cyndie7216/aiserver
          tags: latest
          dockerfile: docker/aiserver/Dockerfile
          cache_froms: cyndie7216/aiserver:latest

  aiprocessor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker build and push
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: cyndie7216/aiprocessor
          tags: latest
          dockerfile: docker/aiprocessor/Dockerfile
          cache_froms: cyndie7216/aiprocessor:latest

  tesseract:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker build and push
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: cyndie7216/tesseract
          tags: latest
          dockerfile: docker/tesseract/Dockerfile
          cache_froms: cyndie7216/tesseract:latest

  tesseractserver:
    runs-on: ubuntu-latest
    needs: [aiserver, tesseract]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker build and push
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: cyndie7216/tesseractserver
          tags: latest
          dockerfile: docker/tesseractserver/Dockerfile
          cache_froms: cyndie7216/tesseractserver:latest

  tesseractprocessor:
    runs-on: ubuntu-latest
    needs: [aiprocessor, tesseract]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker build and push
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: cyndie7216/tesseractprocessor
          tags: latest
          dockerfile: docker/tesseractprocessor/Dockerfile
          cache_froms: cyndie7216/tesseractprocessor:latest

  yolo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker build and push
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: cyndie7216/yolo
          tags: latest
          dockerfile: docker/yolo/Dockerfile
          cache_froms: cyndie7216/yolo:latest

  yoloserver:
    runs-on: ubuntu-latest
    needs: [aiserver, yolo]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker build and push
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: cyndie7216/yoloserver
          tags: latest
          dockerfile: docker/yoloserver/Dockerfile
          cache_froms: cyndie7216/yoloserver:latest

  yoloprocessor:
    runs-on: ubuntu-latest
    needs: [aiprocessor, yolo]
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker build and push
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: cyndie7216/yoloprocessor
          tags: latest
          dockerfile: docker/yoloprocessor/Dockerfile
          cache_froms: cyndie7216/yoloprocessor:latest
